"""You can list the contents of a namespace with dir()"""
import math

# Shows the contents of the global namespace
print(dir())
print('*'*40)

# Shows the contents of the math namespace
print(dir(math))
